import simple, { el, div } from "/simple/simple.js";
import Router from "/simple/Router/Router.js";

simple.stylesheet("/style/styleguide.css");

const router = new Router();

router.add("layout", {
	// activate: 
})

div.c("sections", 

)