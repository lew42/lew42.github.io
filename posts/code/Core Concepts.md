# Object Oriented


# Extending
`extends` keyword, but also plugins, modularity, etc.  Also, object.assign vs $.extend().  All very closely related topics.  Defaults + Overrides.

We extend for 2 reasons:
- to change something that already exists (configure an existing feature)
- to add something that doesn't yet exist (add a new feature)


# Package Manager, Module Marketplace, Version Control, Publishing, Semver
Yea...