// import simple, { el, div, icon, test, assert, write } from "/simple.js";
import { write } from "/simple.js";
import page from "./page.js";

// simple.stylesheet("/first-page/styles.css");

// write("hello world");

// write({
// 	one: "hello",
// 	two: "world"
// })

// write(div(".one.two", "hello world"));

write(page);