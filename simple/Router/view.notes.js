import simple, { el, div, p } from "/simple/simple.js";

export default function(){
//// begin content

el("h1", "RouterView");

p("How do you render sidebars, breadcrumbs, inline, and external links?");

p("How do you have panels?");

el("h2", "Issues");

el("ul",
	el("li", "router.render()?"),
	el("li", "customize views?  extend Route {}?")
)

el("pre", `The real issues:
- extend the `);



} /// end content